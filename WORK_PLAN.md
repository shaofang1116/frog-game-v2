# 《荷塘大冒险 v2》工作计划

更新日期：2026-09-20
状态：Active
权威仓库：`https://github.com/shaofang1116/frog-game-v2`
执行原则：质量优先、单批次只解决一个核心问题、验证后再继续

## 1. 当前目标

在保留 v2 无限纵向地图、平滑镜头、程序化绘图、下沉荷叶、鳄鱼和炸弹玩法的基础上，逐步完成：

- 手机、平板和桌面均可稳定操作；
- 单一实时模式中的连续章节旅程；
- 相同种子和输入产生相同结果；
- 不依赖弹窗的新手教学；
- 可解释的高手计分与事件记录；
- 可恢复的完整状态快照；
- GitHub Pages 和后续 H5 游戏门户可直接部署。

## 2. 权威基线

每次开始工作前必须检查：

1. `WORK_PLAN.md`：当前阶段、边界和下一步；
2. `README.md`：v2 已有能力和操作说明；
3. 根目录 `index.html`：不可修改的 v2 原始基线；
4. `demo/baseline-6376422/index.html`：提交 `6376422` 的冻结副本；
5. `demo/index.html`、`demo/src/` 和 `demo/tests/`：当前实验版本；
6. 当前 Git 分支和工作树状态。

当前基线信息：

- 原始提交：`6376422`；
- 原始 `index.html` SHA-256：`00a079310d941c0238b8dca505811369ef1448b0e60c80088ffccca353f830bd`；
- 实验分支：`feature/v2-journey-input-demo`；
- 当前验收提交：`7ed55dc`；
- 公网试玩：`https://shaofang1116.github.io/frog-game-v2/demo/`；
- GitHub Pages 当前发布源：`feature/v2-journey-input-demo / (root)`。

## 3. 硬性边界

- 原型阶段不得修改根目录 `index.html`；
- 所有实验改动只进入 `demo/`；
- 保持原生 HTML、CSS、JavaScript，不引入构建步骤；
- 核心运行不依赖第三方 CDN 或远程资源；
- 键盘、触屏方向键和 Canvas 滑动必须并存；
- 连续章节间保留分数、时间、炸弹和后续新增的局内状态；
- 相同初始状态、种子、时间步和输入意图必须产生相同结果；
- 随机性只影响可复现的游戏内容或表演，不得破坏公平性；
- 教学由小呱和场景演示完成，不增加强制教学弹窗；
- 每批只推进一个核心问题，完成自动测试和浏览器或真机验证后再继续。

## 4. 状态定义

- `Pending`：尚未开始；
- `In Progress`：当前唯一执行阶段；
- `Needs Verification`：实现完成，但缺少最新验证证据；
- `Blocked`：存在明确外部阻塞；
- `Completed`：已有最新自动化与体验证据。

同一时间只允许一个阶段为 `In Progress`。

## 5. 阶段总览

| 阶段 | 内容 | 状态 |
|---|---|---|
| V0 | v2 权威基线与隔离环境 | Completed |
| V1 | Shift 与触屏长按双格跳 | Completed |
| V2 | 两段连续旅程原型 | Completed |
| V3 | 移动输入可靠性收尾 | Completed |
| V4 | 确定性地图与可重放核心 | Pending |
| V5 | 正式章节状态与完整快照 | Pending |
| V6 | 小呱教学与危险编排 | Pending |
| V7 | 高手计分与事件记录 | Pending |
| V8 | 失败喜剧、蛙格与名场面海报 | Pending |
| V9 | 兼容性、性能与平台发布 | Pending |

## 6. 已完成里程碑

### V0：v2 权威基线与隔离环境

状态：`Completed`

- [x] 确认 `frog-game-v2` 为权威仓库；
- [x] 确认 v2 原始提交 `6376422`；
- [x] 创建隔离分支 `feature/v2-journey-input-demo`；
- [x] 创建 `demo/` 实验副本和冻结基线；
- [x] 通过哈希断言保护根目录和冻结副本；
- [x] 未修改根目录 `index.html`。

### V1：Shift 与触屏长按双格跳

状态：`Completed`

- [x] `Shift + 方向键` 立即跳两格；
- [x] 触屏短按方向键，松开时跳一格；
- [x] 长按 `320ms` 显示两格落点预览；
- [x] 长按松开后跳两格；
- [x] `touchcancel` 清理预览且不触发跳跃；
- [x] 移除独立触屏蓄力按钮和旧 `chargeMode` 路径；
- [x] Canvas 滑动继续保持一格跳；
- [x] 双格跳使用独立动画高度和音效；
- [x] 输入策略已拆分为 `demo/src/input.js`；
- [x] 触屏与键盘行为测试通过；
- [x] 手机公网试玩验收通过。

### V2：两段连续旅程原型

状态：`Completed`

- [x] 保留 v2 绝对 `gridY` 和连续纵向地图；
- [x] 第 12 行进入第二段；
- [x] 第 24 行完成两段旅程；
- [x] 段间不调用 `start()`，保留分数、时间和炸弹；
- [x] HUD 显示当前段数；
- [x] 第二段进入时显示非阻塞提示；
- [x] 最终里程碑显示成功结算；
- [x] 旅程策略拆分为 `demo/src/journey.js`；
- [x] 边界和结构测试通过；
- [x] GitHub Pages 公网发布并完成手机验收。

验收证据：

```text
node --test demo/tests/*.test.cjs
结果：10 tests，10 pass，0 fail

公网地址：
https://shaofang1116.github.io/frog-game-v2/demo/

Pages 构建：
pages-build-deployment #2，Success
```

## 7. 当前阶段：V3 移动输入可靠性收尾

状态：`Completed`

目标：在继续扩展章节和危险前，消除手机连续操作中的非策略性失误。

本阶段按以下顺序分成独立小批次：

1. **单方向输入缓存**
   - [x] 青蛙跳跃期间最多缓存一个下一方向；
   - [x] 落地后只消费一次；
   - [x] 新输入覆盖旧缓存，不形成自动连跳队列；
   - [x] 游戏结束、重开和触摸取消时清空缓存。

2. **多指和重复触摸过滤**
   - [x] 每次 D-Pad 手势只跟踪一个 `touch.identifier`；
   - [x] 第二根手指不得改变当前方向或触发额外跳跃；
   - [x] 过滤触屏结束后浏览器生成的兼容性 `click`；
   - [x] 快速重复触摸不会产生双重跳跃。

3. **手势边界和真机回归**
   - [x] 滑出按钮后取消手势，不残留预览；
   - [x] 页面失焦、旋转和隐藏时清理进行中的触摸；
   - [x] 验证手机竖屏和横屏；
   - [x] 验证炸弹按钮不受方向键长按状态影响，并过滤兼容性 `click`；
   - [x] 验证键盘和 Canvas 滑动没有回归（策略与结构自动化）。

本批本地证据：

- 主触点滑出原按钮后按 `touch.identifier` 取消，错误触点不会干扰；
- `blur`、页面隐藏和 `orientationchange` 统一取消当前长按并清空输入缓存；
- 炸弹触摸与方向长按保持独立，一次触摸不会因兼容性 `click` 消耗两颗炸弹；
- `node --test demo/tests/*.test.cjs`：19 tests，19 pass，0 fail；
- TRAE Browser 扩展已重新初始化，视口 `633x701`、Canvas backing store `960x1402`；
- Browser 原生点击、截图和本页控制台检查通过，控制台无错误；
- 手机竖屏、横屏、真实多指和旋转清理已通过真机验收。

退出条件：

- 连续快速输入不会丢失最近一次明确方向；
- 一次物理触摸最多产生一次跳跃；
- 多指、取消、失焦和旋转不会残留预览或蓄力状态；
- Node 测试、浏览器检查和至少一次真机验收均通过。

## 8. 后续阶段

### V4：确定性地图与可重放核心

状态：`Pending`

- [ ] 引入显式随机种子；
- [ ] 地图生成不直接依赖 `Math.random()`；
- [ ] 记录标准化输入意图和固定时间步；
- [ ] 建立最终状态摘要；
- [ ] 相同种子和输入时间线得到相同地图与状态摘要；
- [ ] 保持每段至少一条可达路线。

### V5：正式章节状态与完整快照

状态：`Pending`

- [ ] 明确 `Run`、`Chapter` 和地图区段的状态所有权；
- [ ] 将两段里程碑扩展为配置化章节；
- [ ] 明确普通死亡、章节重开和继续规则；
- [ ] 保存分数、时间、炸弹、跳跃、击杀和事件记录；
- [ ] 引入带版本号的完整快照；
- [ ] 验证恢复后结果与不中断运行一致。

### V6：小呱教学与危险编排

状态：`Pending`

- [ ] 小呱基础动作和脚本；
- [ ] 通过场景演示长按双格跳；
- [ ] 逐个教学鳄鱼、下沉荷叶和后续危险；
- [ ] 首次安全尝试，重复教学自动缩短；
- [ ] 不新增强制教学弹窗。

### V7：高手计分与事件记录

状态：`Pending`

- [ ] 标记安全、快速和高风险路线；
- [ ] 增加连续跳跃、危险跨越和炸弹多目标奖励；
- [ ] 建立可解释的轻量事件日志；
- [ ] 确保相同种子和相同操作产生相同得分。

### V8：失败喜剧、蛙格与名场面海报

状态：`Pending`

- [ ] 实现低成本失败动画并缩短重复演出；
- [ ] 从真实事件选择高光和意外；
- [ ] 生成正向趣味蛙格称号；
- [ ] 由小呱完成结算鉴定；
- [ ] 生成可保存和分享的名场面海报。

### V9：兼容性、性能与平台发布

状态：`Pending`

- [ ] 手机竖屏、横屏、平板和桌面矩阵；
- [ ] 页面失焦、恢复、旋转和刷新；
- [ ] 低性能设备与长时间运行；
- [ ] Canvas 非空、控件不重叠、控制台无未处理错误；
- [ ] GitHub Pages、ZIP 根目录和相对路径验证；
- [ ] 核心路径无未声明外部请求；
- [ ] 评估 H5 门户 SDK，保持平台适配与核心状态隔离。

## 9. 工具链闸门

在 V4 确定性核心完成前，不评估 Vite、TypeScript、Phaser 或 PixiJS。

后续评估必须回答：

- 当前原生方案出现了什么已量化问题；
- 新工具是否保持 GitHub Pages 静态部署；
- 自动测试和确定性摘要是否一致；
- 包体、请求数和旧移动浏览器兼容性是否可接受；
- 是否存在可立即回退的稳定版本。

没有明确收益或验证失败时，继续使用当前原生方案。

## 10. 每批工作协议

开始前：

1. 阅读“当前检查点”；
2. 执行 `git status --short --branch`；
3. 确认只推进一个最小切片；
4. 写明本批修改范围和明确不修改内容；
5. 先建立会失败的行为测试。

结束前：

1. 运行当前行为测试和完整相关测试；
2. 检查根基线哈希；
3. 检查浏览器控制台、Canvas 和控件布局；
4. 涉及触屏时完成手机公网验收；
5. 更新本计划的阶段状态和变更记录；
6. 未获得用户明确要求时不得提交或推送。

标准验证命令：

```bash
node --test demo/tests/*.test.cjs
shasum -a 256 index.html demo/baseline-6376422/index.html
git status --short --branch
```

## 11. 当前检查点

- 当前阶段：V2 已验收冻结，准备进入 V3；
- 已完成：正确基线、隔离 Demo、Shift 双格跳、触屏长按、两段连续旅程、自动测试、公网真机验收；
- 当前分支：`feature/v2-journey-input-demo`；
- 当前提交：`7ed55dc`；
- 当前发布：GitHub Pages 指向该功能分支；
- 当前阻塞项：无；
- 当前进展：V3 移动输入可靠性收尾已完成并通过真机验收；
- 下一批唯一目标：V4 第 1 小批“显式随机种子”；
- 明确不做：不修改根 `index.html`，不同时开发确定性地图、小呱或新危险。

## 12. 变更记录

| 日期 | 阶段 | 完成内容 | 验证证据 | 风险/偏差 | 下一步 |
|---|---|---|---|---|---|
| 2026-09-18 | V0 | 确认 v2 权威仓库并建立隔离 Demo | 基线哈希、语法与 HTTP 检查 | 旧仓库计划不再适用 | 迁移已验收输入改进 |
| 2026-09-18 | V1 | 完成 Shift 双格跳与触屏长按 | 输入测试、浏览器键盘回归 | 尚未做多指和输入缓存 | 两段连续旅程 |
| 2026-09-20 | V2 | 完成两段旅程并发布 GitHub Pages | 10/10 测试、Pages Success、手机验收 | 当前仅两段里程碑，尚非完整章节系统 | V3 单方向输入缓存 |
| 2026-09-20 | V3.1 | 完成单槽方向缓存、最新输入覆盖、单次消费和边界清理 | RED→GREEN；12/12 测试；浏览器键盘与 Canvas 冒烟 | 浏览器自动化无法稳定制造 238ms 内连续按键，快速时序由单测覆盖 | V3 多指和重复触摸过滤 |
| 2026-09-20 | V3.2 | 完成单触点所有权、次要手指隔离、重复结束幂等和兼容 click 抑制 | RED→GREEN；15/15 测试；浏览器页面、控件、Canvas 与控制台冒烟 | 浏览器接口不支持原生多指注入，仍需真机多指回归 | V3 手势边界和真机回归 |
| 2026-09-20 | V3.3 | 完成滑出取消、失焦/隐藏/旋转清理和炸弹兼容 click 过滤 | RED→GREEN；19/19 测试；Browser 截图与控制台检查；手机竖横屏、多指和旋转真机验收 | 无 | V4 显式随机种子 |
